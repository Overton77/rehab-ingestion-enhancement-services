[project]
name = "rehab-enricher"
version = "0.1.0"
description = "Data enrichment service for rehab app ingestion"
readme = "README.md"
authors = [
    { name = "Overton77", email = "johnoverton743@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "rehab-common",
    "aioboto3>=15.5.0",
    "boto3>=1.40.61", 
     "google-genai>=1.52.0",
    "httpx>=0.28.1",
    "orjson>=3.11.4",
    "pydantic>=2",
    "pydantic-settings>=2",
    "sqlalchemy[asyncio]>=2.0.44",
    "asyncpg>=0.30.0",
    "crawl4ai>=0.7.7",
    "aiofiles>=25.1.0",  
    "openai>=2.8.1",
    "openai-agents>=0.6.1",
]

[project.scripts]
rehab-enricher = "rehab_enricher.__main__:main"

[tool.uv.sources]
rehab-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rehab_enricher"] 


